// const authController = require('../auth/auth.controller');

describe('Rest API Controllers', () => {
  describe('Auth controller', () => {
    it('Should have tests', () => {
      expect(true).toBe(true);
    });

    // @TODO
  });
});
